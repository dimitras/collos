=simple_form_for @race, :html => {:class => 'form-horizontal' } do |f|
    = f.input :name, :hint => "Find the official onomatology of the race" 
    = f.button :submit
    = link_to 'Cancel', @race, class: 'btn btn-warning'
