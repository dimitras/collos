%h3 Register a new account
=form_tag "/auth/identity/register" do 
  - if @identity && @identity.errors.any?
    .error_messages
      %h4
        = pluralize(@identity.errors.count, "error")
        prohibited this account from being saved:
      %ul
        - @identity.errors.full_messages.each do |msg|
          %li= msg
  .field
    = label_tag :name
    = text_field_tag :name, @identity.try(:name)
  .field
    = label_tag :email, "E-mail address"
    = text_field_tag :email, @identity.try(:email)
  .field
    = label_tag :password
    = text_field_tag :password, @identity.try(:password)
  .field
    = label_tag :password_confirmation
    = text_field_tag :password_confirmation, @identity.try(:password_confirmation)
  .actions
    = submit_tag "Register" 