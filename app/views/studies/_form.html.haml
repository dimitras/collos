= simple_form_for @study, :html => {:class => 'form-horizontal'} do |f|
	= f.input :title
	= f.input :identifier
	= f.input :description
	= f.association :investigation
	= f.button :submit
	= link_to 'Cancel', @study, class: 'btn btn-warning'