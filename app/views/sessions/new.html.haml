- unless Rails.env.production?
  %h2 Use a development account
  =button_to("Developer Login", "/auth/developer")

%h2 Log in with Basecamp
=link_to(image_tag("basecamp.png"), url_for("auth/37signals"), class: 'btn btn-large')
