=simple_form_for @sample_characteristics, :html => {:class => 'form-horizontal' } do |f|
    = f.input :sample_id, as: :hidden
    = f.input :name
    = f.input :value
    = f.imput :unit
    = f.button :submit
