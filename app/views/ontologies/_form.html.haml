=simple_form_for @ontology, :html => {:class => 'form-horizontal' } do |f|
    = f.input :name
    = f.input :prefix
    = f.input :uri
    = f.input :description
    = f.button :submit
    = link_to 'Cancel', @ontology, class: 'btn btn-warning'