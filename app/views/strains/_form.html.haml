=simple_form_for @strain, :html => {:class => 'form-horizontal' } do |f|
    = f.input :name, :hint => "Find the official onomatology of the strain" 
    = f.button :submit
    = link_to 'Cancel', @strain, class: 'btn btn-warning'
